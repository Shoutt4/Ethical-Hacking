import {useEffect,useState} from 'react';import {AnimatePresence,motion} from 'framer-motion';import {navItems} from '../data/pentest365';const ids=['inicio','caracteristicas','funcionalidades','cybersoc','partners','clientes','soporte'];export function Brand(){return <a className="brand" href="#inicio" aria-label="Pentest365, inicio"><span className="brand-mark">P</span><span>Pentest<span>365</span></span></a>}export function Navbar(){const[open,setOpen]=useState(false),[scrolled,setScrolled]=useState(false);useEffect(()=>{const fn=()=>setScrolled(scrollY>18);addEventListener('scroll',fn);return()=>removeEventListener('scroll',fn)},[]);return <header className={`nav ${scrolled?'nav-scrolled':''}`}><div className="nav-inner"><Brand/><nav className="desktop-nav" aria-label="Navegación principal">{navItems.map((item,i)=><a href={`#${ids[i]}`} key={item}>{item}</a>)}</nav><div className="nav-actions"><button className="lang">ES <span>⌄</span></button><a href="#cotizacion" className="button small">Cotización <b>↗</b></a><button className="menu-button" aria-label="Abrir menú" aria-expanded={open} onClick={()=>setOpen(!open)}><i></i><i></i></button></div></div><AnimatePresence>{open&&<motion.nav className="mobile-nav" initial={{opacity:0,height:0}} animate={{opacity:1,height:'auto'}} exit={{opacity:0,height:0}}>{navItems.map((item,i)=><a onClick={()=>setOpen(false)} href={`#${ids[i]}`} key={item}>{item}</a>)}<a className="button" href="#cotizacion">Solicitar cotización</a></motion.nav>}</AnimatePresence></header>}
